server:
  port: 8082

spring:
  application:
    name: operation-service
  datasource:
    url: jdbc:postgresql://localhost:5433/operation_db
    username: cerid
    password: password123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  
  # YENİ: RabbitMQ Bağlantı Ayarları
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest